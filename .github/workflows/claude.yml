name: Claude Code

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened, assigned, labeled]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  claude:
    if: |
      github.event_name == 'issues' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude'))
    runs-on: ubuntu-latest

    steps:
      # 1. Определяем dev-ветку из лейбла
      - name: Detect developer branch
        id: dev
        uses: actions/github-script@v7
        with:
          script: |
            let labels = [];
            if (context.payload.issue) {
              labels = context.payload.issue.labels.map(l => l.name);
            } else if (context.payload.pull_request) {
              labels = context.payload.pull_request.labels.map(l => l.name);
            }

            const devLabel = labels.find(l => l.startsWith('developer:'));
            let branch = '';
            let devName = '';
            if (devLabel) {
              devName = devLabel.replace('developer:', '');
              branch = `dev/${devName}`;
            }

            core.setOutput('branch', branch);
            core.setOutput('dev_name', devName);
            console.log(`Developer: ${devName || '(none)'}`);
            console.log(`Target branch: ${branch || 'main (default)'}`);

      # 2. Checkout
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 3. Создаём dev-ветку из main если её ещё нет
      - name: Ensure dev branch exists
        if: steps.dev.outputs.branch != ''
        run: |
          BRANCH="${{ steps.dev.outputs.branch }}"
          if git ls-remote --heads origin "$BRANCH" | grep -q .; then
            echo "✅ Branch $BRANCH already exists"
          else
            echo "🌱 Creating $BRANCH from main"
            git checkout -b "$BRANCH" origin/main
            git push origin "$BRANCH"
            echo "✅ Branch $BRANCH created"
          fi

      # 4. Claude Code — base_branch говорит откуда брать исходники
      #    и куда нацеливать PR
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          base_branch: ${{ steps.dev.outputs.branch || '' }}
          claude_args: |
            --model claude-opus-4-5-20251101
            --max-turns 30

      # 5. Находим ветку Claude, создаём PR если нет, мержим, удаляем
      - name: Auto-merge into dev branch
        if: steps.dev.outputs.branch != ''
        uses: actions/github-script@v7
        with:
          script: |
            const devBranch = '${{ steps.dev.outputs.branch }}';
            const issueNum = context.payload.issue?.number;

            if (!issueNum) {
              console.log('No issue number, skipping');
              return;
            }

            // Даём время на завершение всех операций action
            await new Promise(r => setTimeout(r, 10000));

            // 5a. Ищем ветку Claude по паттерну
            const { data: refs } = await github.rest.git.listMatchingRefs({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: `heads/claude/issue-${issueNum}`
            });

            if (refs.length === 0) {
              console.log(`⚠️ No claude branch found for issue #${issueNum}`);
              return;
            }

            const claudeRef = refs[refs.length - 1]; // последняя (самая свежая)
            const claudeBranch = claudeRef.ref.replace('refs/heads/', '');
            console.log(`Found Claude branch: ${claudeBranch}`);

            // 5b. Ищем существующий PR
            const { data: prs } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: `${context.repo.owner}:${claudeBranch}`,
              state: 'open'
            });

            let pr = prs[0];

            // 5c. Если PR нет — создаём сами, нацеленный на dev-ветку
            if (!pr) {
              console.log(`No PR found, creating one: ${claudeBranch} → ${devBranch}`);
              const { data: newPr } = await github.rest.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                head: claudeBranch,
                base: devBranch,
                title: `Auto-merge: issue #${issueNum}`,
                body: `Automatic merge of Claude's work into \`${devBranch}\`.\n\nCloses #${issueNum}`
              });
              pr = newPr;
              console.log(`✅ Created PR #${pr.number}`);
            } else {
              console.log(`Found existing PR #${pr.number}: ${pr.head.ref} → ${pr.base.ref}`);
              // Перенаправляем на dev-ветку если нацелен на main
              if (pr.base.ref !== devBranch) {
                await github.rest.pulls.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: pr.number,
                  base: devBranch
                });
                console.log(`✅ Retargeted PR #${pr.number} → ${devBranch}`);
              }
            }

            // 5d. Мержим
            try {
              await github.rest.pulls.merge({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
                merge_method: 'squash',
                commit_title: `${pr.title} (#${pr.number})`
              });
              console.log(`✅ Merged PR #${pr.number} into ${devBranch}`);
            } catch (err) {
              console.log(`❌ Merge failed: ${err.message}`);
              return; // не удаляем ветку если мерж не удался
            }

            // 5e. Удаляем временную ветку Claude
            try {
              await github.rest.git.deleteRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: `heads/${claudeBranch}`
              });
              console.log(`🗑️ Deleted branch ${claudeBranch}`);
            } catch (err) {
              console.log(`⚠️ Could not delete branch: ${err.message}`);
            }
